# ReactJs with TypeScript in Spring Boot

## Available Scripts

### In the project directory, you can run the server:

- `./mvnw clean spring-boot:run`

## Access to the application

- in a browser, go to `http://localhost:8080/`